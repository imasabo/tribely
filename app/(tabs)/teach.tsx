import { View } from 'react-native';

/** Placeholder — Teach tab opens /create via custom TabBar */
export default function TeachPlaceholder() {
  return <View className="flex-1 bg-background" />;
}
